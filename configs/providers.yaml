# 智能体提供商配置
# 每个智能体可以独立配置 LLM 提供商

defaults: &defaults
  provider: openai
  model: gpt-4o
  base_url: https://api.openai.com/v1
  api_key: ""
  temperature: 0.7
  max_tokens: 4096

outline_writer:
  <<: *defaults

script_writer:
  <<: *defaults

script_converter:
  <<: *defaults
